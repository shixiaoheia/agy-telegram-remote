# 安全边界

## 适用场景

只允许本人或完全信任的人使用，建议专用 VPS。此项目是远程执行入口，不是面向不可信租户的托管平台。新安装默认开启 `--dangerously-skip-permissions`，代表工具调用自动审批，不代表操作安全或正确。

## 本版本实施的措施

### 权限模式

下文 systemd 加固措施仅适用于默认的 `AGY_HOST_ACCESS=restricted`。管理员可明确使用安装器 `--full-host-access` 切换到 `full` 并开启自动审批。完整权限模式关闭 `ProtectSystem`、`PrivateTmp`、`NoNewPrivileges`、`RestrictSUIDSGID`，不设置 `ReadWritePaths` 或清空 capabilities；Agent 获得宿主机允许的 root 权限，能够修改系统文件、服务和凭据。所有白名单账户与模型任务均共享此权限，不具备主机隔离保证。私聊、白名单、配置校验、结果保存和取消清理仍然保留。普通升级保留模式，`--restricted-host-access` 恢复默认加固。自定义 drop-in 或宿主机限制仍可能生效。

程序发布目录为 root 所有，Bot 使用系统 Python，禁用用户 Python site 与 Python 环境覆盖；专为个人独立 VPS 设计，以 root 身份原生运行于 `/root` 主目录，并通过 systemd `ProtectSystem=strict`、`0600` 私有文件权限与私有临时目录进行安全隔离。旧版用户可写虚拟环境不会被特权更新器执行。

配置不被 shell source/eval。目录、链接、配置类型、白名单和限额被检查。服务采用 `NoNewPrivileges`、只读系统目录、受限可写路径及私有临时目录。私聊和数字 ID 是任务入口检查条件。

`AGY_WRITE_PATHS` 默认留空。安装器 `--write-paths` 可显式开放最多 16 个具体应用目录，普通升级保留原列表；不会关闭 `ProtectSystem=strict`、`NoNewPrivileges` 或恢复被清空的 capabilities。目录要求已存在、无符号链接，不允许根目录或整个顶层目录，也不改变目标目录所有权与访问权限。授权对所有白名单用户生效，不能把它当作逐用户隔离。`/status` 只展示配置值，实际 systemd drop-in 与挂载状态需在服务器检查。

agy 子进程只继承有限环境，不自动继承 Bot Token、`PYTHONPATH`、`SSH_AUTH_SOCK` 或无关云凭据。结果文件使用私有目录和 `0600`，先持久保存再投递。日志不主动保存任务原文、原始 stdout/stderr、Token 或授权码；已知 Bot Token 的原文会被替换。

**新增功能的专属安全控制：**
- **动态白名单权限隔离与防踢保护**：动态白名单仅主管理员（`ALLOWED_USER_IDS` 首位 ID）有权增删，以 `0600` 权限独立保存在 `state/whitelist.json`。指令执行时严格验证 5-16 位数字 ID 格式。静态基础配置中的管理员享受防误踢保护，系统严禁通过 `/whitelist remove` 移除基础配置用户，杜绝误操作导致管理员锁死。
- **安全平滑重启互斥锁**：`/restart` 在执行前强制检查任务槽位。若当前有任务正在运行，强制阻断重启请求，杜绝暴力中断运行中任务引发的状态损坏。
- **工作区文件速览防越权遍历（`/ls`）**：严格进行绝对路径规范化校验（`Path.resolve()` 与 `Path.relative_to()`），彻底封堵含有 `..` 路径遍历逃逸、越权探测与工作区外的非法符号链接。
- **CLI 参数安全白名单**：透传至 `agy` 进程的 `--model` 严格受正则 `[A-Za-z0-9._-]{2,64}` 约束；`--effort` 与 `--mode` 严格受静态允许枚举值约束，从源头彻底杜绝 Shell 命令注入。

## 明确不保证的事情

**高信任 Root 原生执行环境，非容器沙箱。**
本项目专为个人独立专用 VPS 打造，以 root 身份原生运行于宿主机，且默认启用 `--dangerously-skip-permissions`（自动审批工具调用）。这意味着：
- **白名单账户接管影响**：如果白名单中的某个 Telegram 账户被接管，或者通过模型触发了成功的提示词注入（Prompt Injection），攻击者可以在服务权限范围内以 root 执行操作，读取敏感文件并修改可写目录。只读挂载与被清空的 capabilities 限制了部分写入和管理操作，但不构成不可信代码的隔离保证。
- **严禁向不可信人员开放**：绝对不要将任何不受信任的 Telegram 用户加入白名单。所有白名单用户共享服务的执行权限与额外可写目录。
- **私聊与数字 ID 校验为强安全防线**：系统严格限制仅私聊且必须在白名单（数字 ID 5-16 位）内的用户才能发送指令与交互，杜绝任何群聊扩散或未授权访问。

**固定回复自检不是强制只读环境。** 自检提示明确要求不用工具、不改文件，但为了验证实际调用链，它采用与普通任务相同的权限配置。提示文字不构成安全隔离；工作目录的规则或 hooks 也可能影响自检行为。

**同 UID 不提供凭据隔离。** agy 与 Bot 同为 root 运行，可以读取该账户有权访问的所有宿主机文件，包括 Bot 配置、Google 凭据与其他白名单用户的结果。过滤环境变量并不能解决同 UID 文件或进程访问。Google 凭据本身必须能被 agy 访问。结果正文还可能含用户要求读取的其他敏感内容，不能依赖通用脱敏保证。

**工作目录不是文件系统沙箱。** `cwd` 不限制读取范围。systemd 加固收紧部分系统写入范围，但不阻断 root 账户本来可读的所有路径，也不限制网络请求。程序未启用 Google CLI 的内部沙箱，未实现容器级别文件与网络隔离。

**自动审批可能导致误删、泄密或其他副作用。** 文件、网页和工具输出可能含提示注入；模型也可能误解合法任务。由于以 root 身份运行且默认开启自动审批，不要在工作目录放置不相关的机密凭据，白名单账户之间必须完全互信。

**进程组清理不是每任务 cgroup。** 正常子进程、父进程先退出后的同组子进程、保留管道的子进程都会被清理。超时先 SIGTERM，再按需 SIGKILL。若管道不能关闭或清理未确认，暂停接收新任务，要求检查并重启服务。通过 `setsid()` 等方式脱离进程组、又关闭继承管道的进程可能无法被这一机制发现；不要用任务启动长期守护进程。停止服务时由 systemd 的整个服务控制组清理其成员。更强要求需要独立的每任务 cgroup/容器设计。

**不保证精确一次执行或业务正确。** 更新水位先保存，崩溃窗口可能丢任务；不自动重试避免重复副作用。`SUCCESS` 代表 CLI 协议层状态，并非验证所有业务动作都正确。`/cancel` 不会撤销已经执行的操作。

**资源限制不是全系统配额。** 捕获输出和保存结果有上限，但工作目录、Google 缓存、系统日志、发布目录与 root 备份不受该上限控制。root 备份和历史发布不会自动删除，管理员应核对后定期清理并保护备份。进程仍可能占用大量 CPU、磁盘或网络。

**更新与供应链边界。** 
- 安装与更新脚本默认从配置的 Git 仓库获取代码，并以 root 身份下载 Google 官方安装脚本。
- **不可变版本升级推荐**：为杜绝依赖分支浮动带来的供应链风险，强烈建议生产环境使用不可变的 Commit SHA 进行升级，例如 `bash install.sh --ref <commit-sha>`。安装器会自动在 `$release/.commit` 及 `/etc/agy-telegram-remote/current_commit` 中固化记录当前提交并在完成时高亮展示。
- **Google CLI 安装脚本校验**：安装器从 `https://antigravity.google/cli/install.sh` 下载官方脚本时，会强制验证 HTTPS、脚本完整性、bash 头部以及官方域名结构特征，并支持通过 `AGY_INSTALLER_SHA256` 环境变量指定精确校验哈希，校验不通过立即中止，绝不盲目执行。
- GitHub Actions 持续集成使用固定 commit 哈希锁定 actions（如 checkout v4、setup-python v5），并禁用凭据持久化。

## 处理泄露

泄露 Bot Token 时立即通过 BotFather 撤销/重置，并更新服务器配置。泄露 Google 凭据时在相应账户侧撤销访问或重新授权。不要把凭据粘贴到 GitHub Issue、聊天截图或公开日志中。

可以公开报告不含敏感信息的复现步骤；涉及凭据的报告只描述凭据类型、影响和已做的撤销操作，不附真实值。
